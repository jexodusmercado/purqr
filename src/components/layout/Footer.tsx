export function Footer() {
  return (
    <footer className="py-4 px-6 border-t border-gray-200 text-center text-sm text-gray-500">
      © {new Date().getFullYear()} purqr — Generate QR codes in PNG, SVG, and PDF
    </footer>
  );
}
